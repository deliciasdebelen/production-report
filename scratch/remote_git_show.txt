================================================================================
COMMAND: cd /home/administrador/apps/production-report && git show HEAD:docker-compose.yml
================================================================================
version: '3.8'

services:
  web:
    build: .
    container_name: production-report
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      # Mount database directory for persistence
      - ./data:/app/data
      # Mount production.db in the app root
      - ./production.db:/app/production.db
      # Mount application code for hot-reloading & production fixes
      - ./app:/app/app
      # Fix SSL Legacy issues
      - ./custom_openssl.cnf:/etc/ssl/openssl.cnf
    environment:
      - PYTHONUNBUFFERED=1
      - DATABASE_URL=sqlite:////app/production.db
      - OPENSSL_CONF=/etc/ssl/openssl.cnf
    healthcheck:
      test: [ "CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/login').read()" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

================================================================================
COMMAND: cd /home/administrador/apps/production-report && git show HEAD:app/database.py || true
================================================================================
from sqlalchemy import create_engine
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import sessionmaker

import os

# Use environment variable if set, otherwise definitively fallback to PostgreSQL
# to ensure no SQLite instances are ever recreated.
_db_url = os.getenv("DATABASE_URL", "")

if os.getenv("TESTING") == "1":
    SQLALCHEMY_DATABASE_URL = _db_url or "sqlite:///./test.db"
elif not _db_url or "sqlite" in _db_url:
    # Ensure no local SQLite instances are recreated in production
    SQLALCHEMY_DATABASE_URL = "postgresql://app_user:production_password@db:5432/production_db"
else:
    SQLALCHEMY_DATABASE_URL = _db_url

engine = create_engine(SQLALCHEMY_DATABASE_URL)
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

Base = declarative_base()

def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()

================================================================================
COMMAND: cd /home/administrador/apps/production-report && git diff HEAD docker-compose.yml || true
================================================================================
diff --git a/docker-compose.yml b/docker-compose.yml
old mode 100644
new mode 100755
index 1b9312f..a156174
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -1,34 +1,50 @@
-version: '3.8'
-
-services:
-  web:
-    build: .
-    container_name: production-report
-    restart: unless-stopped
-    ports:
-      - "8000:8000"
-    volumes:
-      # Mount database directory for persistence
-      - ./data:/app/data
-      # Mount production.db in the app root
-      - ./production.db:/app/production.db
-      # Mount application code for hot-reloading & production fixes
-      - ./app:/app/app
-      # Fix SSL Legacy issues
-      - ./custom_openssl.cnf:/etc/ssl/openssl.cnf
-    environment:
-      - PYTHONUNBUFFERED=1
-      - DATABASE_URL=sqlite:////app/production.db
-      - OPENSSL_CONF=/etc/ssl/openssl.cnf
-    healthcheck:
-      test: [ "CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/login').read()" ]
-      interval: 30s
-      timeout: 10s
-      retries: 3
-      start_period: 40s
-    networks:
-      - app-network
-
-networks:
-  app-network:
-    driver: bridge
+version: '3.8'
+
+services:
+  production_report_web:
+    build: .
+    container_name: production-report
+    restart: unless-stopped
+    ports:
+      - "8000:8000"
+    volumes:
+      # Mount database directory for persistence
+      - ./data:/app/data
+      # Mount application code for hot-reloading & production fixes
+      - ./app:/app/app
+      # Fix SSL Legacy issues
+      - ./custom_openssl.cnf:/etc/ssl/openssl.cnf
+    environment:
+      - PYTHONUNBUFFERED=1
+      - DATABASE_URL=postgresql://app_user:production_password@production_report_db:5432/production_db
+      - OPENSSL_CONF=/etc/ssl/openssl.cnf
+    depends_on:
+      production_report_db:
+        condition: service_started
+    healthcheck:
+      test: [ "CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/login').read()" ]
+      interval: 30s
+      timeout: 10s
+      retries: 3
+      start_period: 40s
+    networks:
+      - app-network
+
+  production_report_db:
+    image: postgres:15
+    container_name: production-report-db
+    restart: unless-stopped
+    environment:
+      POSTGRES_USER: app_user
+      POSTGRES_PASSWORD: production_password
+      POSTGRES_DB: production_db
+    volumes:
+      - ./pgdata:/var/lib/postgresql/data
+    ports:
+      - "5436:5432"
+    networks:
+      - app-network
+
+networks:
+  app-network:
+    driver: bridge

